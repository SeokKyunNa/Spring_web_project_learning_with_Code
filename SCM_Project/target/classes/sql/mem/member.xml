<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
<!-- 아디중복체크 -->
    <select id="login_check1" parameterType="String" resultType="member">
     select * from join_member where join_id=#{id}
    </select>
    <!-- 비번 검색 -->
    <select id="pwd_find" resultType="member" parameterType="java.util.Map">
     select join_pwd  from join_member 
     where join_id=#{id} and join_name=#{name}
    </select>
    
    <!-- 회원저장 -->
    <insert id="member_join" parameterType="member">
     insert into join_member (join_code,join_id,join_pwd,join_name
     ,join_email,join_date)
      values(join1_member_joincode_seq.nextval,
     #{join_id},#{join_pwd},#{join_name},
         #{join_email},SYSDATE)
    </insert>
    
    <!-- 프로필 생성 -->
    <insert id="prf" parameterType="member">
    
       insert into PROFILE_TABLE (PROFILE_ID,PROFILE_USER,PROFILE_AGE,PROFILE_EDU,
       PROFILE_INTRO,PROFILE_LANGU,
      PROFILE_INTEREST,PROFILE_CARRER, PROFILE_LOCAL,
      PROFILE_LISENCE,PROFILE_BOOLEAN) 
      values
      (#{join_id},#{join_name},' ',' ',
      ' ',' ',
      ' ',' ',' ',
      ' ',' ')
    </insert>
    
    <!-- 로그인 인증 -->
    <select id="login_check" parameterType="String" resultType="member">
     select * from join_member where join_id=#{id} and join_pwd=#{pwd}
    </select>
    
    <!-- 회원수정 -->
    <update id="member_edit" parameterType="member">
     update join_member set join_pwd=#{join_pwd},join_name=#{join_name},
    
     join_email=#{join_email}
     where join_id=#{join_id}
    </update>    
    
    <!-- 회원삭제 -->
    <update id="member_delete" parameterType="member">
      update join_member set join_delcont=#{join_delcont}, join_state=2,
      join_deldate=sysdate where join_id=#{join_id}
    </update>
</mapper>








